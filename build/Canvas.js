// Generated by CoffeeScript 1.6.3
(function() {
  var global;

  global = this;

  this.CanvasView = (function() {
    function CanvasView(canvasElement) {
      this.subviews = [];
      this.ctx = canvasElement.getContext("2d");
    }

    CanvasView.prototype.draw = function() {
      var self, view, _i, _len, _ref, _results;
      self = this;
      self.ctx.clearRect(0, 0, self.ctx.canvas.width, self.ctx.canvas.height);
      _ref = this.subviews;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        view = _ref[_i];
        _results.push((function(view) {
          return view.draw(self.ctx);
        })(view));
      }
      return _results;
    };

    return CanvasView;

  })();

  this.Circle = (function() {
    function Circle() {
      this.position = new Vector;
      this.radius = 5;
    }

    Circle.prototype.draw = function(ctx) {
      var self;
      self = this;
      ctx.beginPath();
      ctx.arc(self.position.x, self.position.y, self.radius, 0, 2 * Math.PI);
      return ctx.fill();
    };

    return Circle;

  })();

}).call(this);
